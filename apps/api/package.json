{
	"name": "api",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "wrangler dev --local --port 3000",
		"build-prod": "vite build --mode production --ssr src/cf-worker.ts",
		"build-dev": "vite build --mode development --ssr src/cf-worker.ts"
	},
	"keywords": [],
	"author": "",
	"license": "MIT",
	"dependencies": {
		"@badrap/valita": "^0.1.9",
		"gen-utils": "workspace:^0.0.4",
		"hono": "^2.7.2"
	},
	"devDependencies": {
		"concurrently": "^7.6.0",
		"wrangler": "^2.7.1"
	}
}
